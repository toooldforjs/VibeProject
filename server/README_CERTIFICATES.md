# Настройка сертификатов Минцифры для GigaChat

Для работы с GigaChat API необходимо установить корневой сертификат Минцифры.

## Автоматическая установка

При первом запуске сервера сертификат будет автоматически загружен с официального источника и сохранен в `server/certs/russian_trusted_root_ca_pem.crt`.

## Ручная установка

Если автоматическая загрузка не работает, выполните следующие шаги:

### 1. Скачайте сертификат

Перейдите на [портал Госуслуг](https://www.gosuslugi.ru/crt) и скачайте корневой сертификат в формате PEM.

Или используйте команду:

```bash
curl -k "https://gu-st.ru/content/lending/russian_trusted_root_ca_pem.crt" -o server/certs/russian_trusted_root_ca_pem.crt
```

### 2. Создайте директорию для сертификатов

```bash
mkdir -p server/certs
```

### 3. Поместите сертификат

Поместите скачанный файл `russian_trusted_root_ca_pem.crt` в директорию `server/certs/`.

## Проверка установки

После установки сертификата перезапустите сервер. В логах должно появиться сообщение:

```
✅ Корневой сертификат Минцифры найден
🔐 NODE_EXTRA_CA_CERTS установлен: <путь к сертификату>
```

## Использование в коде

Сертификаты автоматически используются при работе с GigaChat API через модуль `server/utils/certificates.js`. HTTPS-агент создаётся функцией `createHttpsAgent()` и передаётся в клиент GigaChat (см. `server/routes/gigachat.js`). Отдельно вызывать или подставлять агент в запросы не требуется.

## Устранение проблем

### Ошибка: `[SSL: CERTIFICATE_VERIFY_FAILED]`

Это означает, что сертификат не установлен или не найден. Убедитесь, что:

1. Файл сертификата находится в `server/certs/russian_trusted_root_ca_pem.crt`
2. Сервер был перезапущен после установки сертификата
3. Права доступа к файлу корректны

### Ошибка при загрузке сертификата

Если автоматическая загрузка не работает:

1. Проверьте интернет-соединение
2. Скачайте сертификат вручную с https://www.gosuslugi.ru/crt
3. Поместите его в `server/certs/russian_trusted_root_ca_pem.crt`

## Дополнительная информация

- [Документация GigaChat по сертификатам](https://developers.sber.ru/docs/ru/gigachat/certificates)
- [Портал Госуслуг - Сертификаты](https://www.gosuslugi.ru/crt)
